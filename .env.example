# Absolute or repo-relative path to your KMyMoney file (.kmy, gzipped XML)
KMYMONEY_FILE=./data/bgt.kmy

# Optional: set to "false" to require an explicit kmy_save call after mutations.
# Default is "true" (every successful mutation is immediately persisted with a rolling backup).
KMY_AUTOSAVE=true
